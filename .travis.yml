language: python
python:
  - 2.7
install:
  - pip install numpy
  - pip install pandas
  - pip install sklearn
  - pip install matplotlib
  - pip install seaborn
script:
  - pyicics
os: 
   windows